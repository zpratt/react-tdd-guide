'use strict';

var ButtonDropdown = require('./button-dropdown-component.jsx'),

    React = require('react');

module.exports = React.createClass({
    render: function () {
        return (
            <div>
                <ButtonDropdown />
            </div>
        );
    }
});
